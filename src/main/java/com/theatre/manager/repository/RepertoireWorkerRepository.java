package com.theatre.manager.repository;

import com.theatre.manager.model.RepertoireWorker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepertoireWorkerRepository extends JpaRepository<RepertoireWorker, Long> {
}
